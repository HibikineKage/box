import { IPlayer } from '../player/ducks';

const player: IPlayer = {
  x: 10,
  y: 100,
  vy: 0,
  isJumping: false,
  isFacingRight: false,
  name: 'Hibikine',
  playerId: '0',
};
describe('game reducer', () => {
  test('tick', () => {});
});
